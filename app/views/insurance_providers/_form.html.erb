<%= form_with(model: insurance_provider) do |form| %>
  <% if insurance_provider.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(insurance_provider.errors.count, "error") %> prohibited this insurance_provider from being saved:</h2>

      <ul>
        <% insurance_provider.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
