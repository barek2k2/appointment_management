<%= form_with(model: billing_method) do |form| %>
  <% if billing_method.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(billing_method.errors.count, "error") %> prohibited this billing_method from being saved:</h2>

      <ul>
        <% billing_method.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
